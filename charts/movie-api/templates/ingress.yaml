{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "movie-api-chart.fullname" . }}-ingress
  labels:
    name: {{ include "movie-api-chart.fullname" . }}-ingress
{{ include "movie-api-chart.labels" . | indent 4 }}
{{- with .Values.ingress.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if .Values.ingress.ingressClassName }}
  ingressClassName: {{ .Values.ingress.ingressClassName }}
{{- end }}
  rules:
  - host: {{ trimPrefix "https://" .Values.ingress.url }}
    http:
      paths:
      - path: '/'
        pathType: Prefix
        backend:
          service:
            name: {{ include "movie-api-chart.fullname" . }}
            port:
              number: 8009
  tls:
  - hosts:
    - {{ trimPrefix "https://" .Values.ingress.url }}
    secretName: {{ include "movie-api-chart.fullname" . }}-ingress-lets-encrypt
{{- end -}}
